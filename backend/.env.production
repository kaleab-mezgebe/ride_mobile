# Niyat Ride Backend - Production Environment Variables for Render
# Copy this template and set these environment variables in Render

# --- Database (PostgreSQL with Render's PostgreSQL) ---
# Replace with your Render PostgreSQL database URL
DB_URL=jdbc:postgresql://your-postgres-host:5432/your-database-name?sslmode=require
DB_USERNAME=your_database_username
DB_PASSWORD=your_database_password

# --- JWT configuration ---
# Generate a secure random secret (at least 64 characters)
JWT_SECRET=your_secure_jwt_secret_at_least_64_characters_long
JWT_EXPIRATION=3600000

# --- Afro Message API ---
# Your Afro Message API token
AFROMESSAGE_API_TOKEN=your_afro_message_api_token

# --- Default admin seeding ---
NYAT_ADMIN_EMAIL=admin@niyat.com
NYAT_ADMIN_PASSWORD=your_secure_admin_password
NYAT_ADMIN_PHONE=+251900000000

# --- Server Port ---
# Render will automatically set PORT environment variable
# Default fallback is 8080
